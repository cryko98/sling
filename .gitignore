.DS_Store
Thumbs.db
desktop.ini
.vercel
.idea/
.vscode/
*.log
node_modules/

# real keys never go in the repo — .env.example is the template
.env
.env.local
.env*.local

# Original unsorted source images — kept on disk, but they are byte-identical
# duplicates of the renamed files in assets/, so they stay out of the repo.
photo_*.jpg
slinglogo.jpg
